# 开发指南

本文档提供完整的项目开发工作流，包括环境配置、常见开发场景和故障排除。

## 目录

- [初始化环境](#初始化环境)
- [开发命令](#开发命令)
- [常见开发场景](#常见开发场景)
- [项目结构](#项目结构)
- [调试技巧](#调试技巧)
- [故障排除](#故障排除)

## 初始化环境

### 系统要求

- **macOS 13+** / **Linux (Ubuntu 20.04+)** / **Windows 10+**
- **Node.js** ≥ 20.x
- **Python** ≥ 3.14
- **Rust** 最新稳定版
- **uv** Python 包管理工具

### Windows 开发环境配置

**重要：** 在 Windows 系统上进行开发时，为了解决 Mac 与 Windows 系统的换行符差异问题，必须运行以下 git 配置命令：

```bash
git config core.autocrlf false
```

这个命令确保 git 不会自动转换换行符，避免在不同操作系统间协作时出现不必要的文件差异。

### 一键初始化（推荐）

```bash
# 克隆项目
git clone https://github.com/TexasOct/iDO.git
cd iDO

# macOS / Linux
pnpm setup

# Windows
pnpm setup:win

# 或手动运行所有初始化步骤
pnpm setup-all
```

**自动执行的步骤：**
1. ✅ 安装 Node.js 依赖 (`pnpm install`)
2. ✅ 初始化 Python 虚拟环境 (`uv sync` - 在项目根目录创建 `.venv`)
3. ✅ 验证国际化翻译 (`pnpm check-i18n`)

### 手动初始化
## 项目结构概览

```
ido/
├── src/                    # 前端 React 代码
├── src-tauri/             # Tauri 应用配置
│   ├── python/            # Python 后端代码（PyTauri）
│   ├── src/               # Rust 代码
│   └── Cargo.toml         # Rust 依赖
├── package.json           # Node.js 依赖
├── pyproject.toml         # Python 项目配置（项目根目录）
├── pnpm-lock.yaml         # pnpm 锁定文件
└── docs/                  # 文档
```

## 前置要求

- **Node.js**: v20 或以上
- **Rust**: 最新稳定版（[安装 Rust](https://rustup.rs/)）
- **Python**: 3.14 或以上
- **uv**: Python 包管理工具（[安装 uv](https://docs.astral.sh/uv/getting-started/installation/)）
- **pnpm**: Node.js 包管理工具（`npm install -g pnpm`）

### PyTAURI 环境配置

在开始本项目开发之前，请确保已按照 [PyTAURI 官方教程](https://pytauri.github.io/pytauri/latest/usage/tutorial/) 配置了基础的 PyTAURI 开发环境。官方教程涵盖了：

- PyTAURI 的基础概念和架构
- Python 与 Rust 的桥接配置
- 基础开发环境设置
- 样例项目的创建和运行

完成官方教程的基础配置后，请继续阅读本文档的后续部分，了解本项目特有的配置要求和最佳实践。

## 前端环境配置

### 1. 安装 Node.js 依赖

```bash
# 1. 安装前端依赖
pnpm install

# 2. 初始化 Python 环境（项目根目录）
uv sync

# 3. 验证国际化翻译
pnpm check-i18n
```

## 开发命令

### 前端开发服务器

```bash
# 启动前端开发服务器（仅热重载 React）
pnpm dev

# 访问：http://localhost:5173
```

**特点：**
- ✅ 快速热重载（HMR）
- ✅ 不需要编译 Rust/Python
- ✅ 修改 React 代码立即看到效果

### Tauri 完整应用开发

```bash
# 推荐：启动 Tauri 开发模式 + 自动生成 TypeScript 客户端
pnpm tauri:dev:gen-ts

# 或普通启动（仅修改前端）
pnpm tauri dev
```

**特点：**
- ✅ 完整的桌面应用环境
- ✅ 前端热重载
- ✅ 自动生成 PyTauri TypeScript 客户端
- ✅ 实时查看 Rust/Python 编译错误

### FastAPI 后端开发

```bash
# 启动独立 FastAPI 服务器（仅后端，无需 Tauri）
uvicorn app:app --reload

# 或使用 uv
uv run python app.py

# API 文档：http://localhost:8000/docs
```

**特点：**
- ✅ 快速调试后端 API
- ✅ 自动 API 文档生成（Swagger UI）
- ✅ 无需启动完整的 Tauri 应用

### 代码质量检查

```bash
# 格式化代码（Prettier + Black）
pnpm format

# 代码检查（ESLint + Python linter）
pnpm lint

# 检查国际化翻译一致性
pnpm check-i18n

# 构建生产版本
pnpm tauri build

# 清理构建产物
pnpm clean
```

### 其他实用命令

```bash
# 重新同步 Python 后端（添加新模块后运行）
pnpm setup-backend

# 构建分发包（仅限 macOS/Linux）
pnpm bundle

# 构建分发包（仅限 Windows）
pnpm bundle:win

# 预览生产构建
pnpm preview

# 只运行 TypeScript 编译检查
pnpm tsc
```

## 常见开发场景

### 场景 1：仅修改前端 UI

最快的开发流程。

```bash
pnpm dev
# 访问 http://localhost:5173
```

**流程：**
1. 修改 React/TypeScript 代码
2. 浏览器自动热重载
3. 无需编译 Rust/Python

### 场景 2：修改前端和后端代码

需要使用 Tauri 应用来测试完整功能。

```bash
pnpm tauri:dev:gen-ts
```

**流程：**
1. 修改 Python 或 TypeScript 代码
2. Tauri 自动编译
3. 前端热重载
4. PyTauri 自动重新生成 TypeScript 客户端（如修改后端接口）

### 场景 3：添加新的 Python 模块

在 `backend/` 目录中添加新模块时。

```bash
# 1. 创建新模块文件
# 例如：backend/my_module.py

# 2. 在 src-tauri/python/ido_app/__init__.py 中导入
# from . import my_module

# 3. 重新同步 Python 环境
pnpm setup-backend

# 4. 重启 Tauri 应用
pnpm tauri dev
```

### 场景 4：独立开发后端 API

无需 Tauri 桌面应用，快速开发和测试 API。

```bash
# 启动 FastAPI 服务器
uvicorn app:app --reload

# 在另一个终端测试 API
curl http://localhost:8000/docs
```

**特点：**
- ✅ 快速迭代后端逻辑
- ✅ 自动 API 文档生成
- ✅ 无需编译 Rust

### 场景 5：调试特定的后端处理器

创建新的 API Handler 或修改现有处理器。

```bash
# 1. 修改 backend/handlers/your_handler.py
# 2. FastAPI 自动重载
# 3. 访问 http://localhost:8000/docs 查看新端点
```

参考 [API Handler 系统文档](./api_handler.md) 了解更多。

### 场景 6：修改国际化翻译

添加或更新多语言支持。

```bash
# 1. 编辑 src/locales/en.ts（英文是源文件）
# 2. 编辑 src/locales/zh-CN.ts（对应翻译）
# 3. 验证翻译一致性
pnpm check-i18n

# 4. 运行应用查看效果
pnpm dev
```

详见 [国际化指南](./i18n.md)

### 场景 7：构建生产版本

打包为可发布的桌面应用。

```bash
# 1. 确保代码通过所有检查
pnpm lint
pnpm check-i18n

# 2. 构建应用
pnpm tauri build

# 3. 输出文件在 src-tauri/target/release/bundle/
```

## 项目结构

### 完整目录树

```
ido/
├── src/                              # React 前端源代码
│   ├── views/                       # 页面级组件（路由目标）
│   │   ├── Activity/
│   │   ├── Dashboard/
│   │   └── Settings/
│   ├── components/                  # 可复用组件
│   ├── layouts/                     # 布局组件
│   ├── lib/
│   │   ├── stores/                  # Zustand 状态管理
│   │   │   ├── activity.ts         # 活动时间轴数据
│   │   │   ├── agents.ts           # Agent 任务
│   │   │   ├── dashboard.ts        # 仪表板数据
│   │   │   ├── settings.ts         # 设置和配置
│   │   │   └── ui.ts               # UI 状态
│   │   ├── services/                # API 服务层
│   │   │   ├── activity/
│   │   │   ├── agents/
│   │   │   └── system/
│   │   ├── types/                   # TypeScript 类型定义
│   │   ├── config/                  # 配置文件
│   │   │   └── menu.ts             # 菜单配置（驱动路由和 UI）
│   │   ├── client/                  # 自动生成的 PyTauri 客户端
│   │   └── hooks/                   # 自定义 React hooks
│   └── locales/                     # i18n 翻译文件
│       ├── en.ts                   # 英文（源文件）
│       └── zh-CN.ts                # 中文
│
├── src-tauri/                        # Tauri 桌面应用配置
│   ├── python/                      # Python 后端代码
│   │   └── ido_app/
│   │       ├── handlers/            # API 处理器（@api_handler 装饰器）
│   │       │   ├── greeting.py
│   │       │   ├── perception.py
│   │       │   ├── processing.py
│   │       │   ├── agents.py
│   │       │   └── system.py
│   │       ├── models/              # Pydantic 数据模型
│   │       │   ├── requests.py
│   │       │   ├── responses.py
│   │       │   └── base.py
│   │       ├── db/                  # 数据库操作
│   │       └── __init__.py          # PyTauri 入口点
│   ├── src/                         # Rust 代码
│   └── Cargo.toml                   # Rust 依赖
│
├── backend/                          # Python 后端代码（符号链接到 src-tauri/python）
│   ├── handlers/                    # API 处理器
│   ├── models/                      # 数据模型
│   └── db/                          # 数据库
│
├── docs/                            # 详细文档
│   ├── development.md               # 本文件 - 开发工作流
│   ├── backend.md                   # 后端架构设计
│   ├── frontend.md                  # 前端架构设计
│   ├── architecture.md              # 系统架构概览
│   ├── i18n.md                      # 国际化配置
│   ├── fastapi_usage.md             # FastAPI 使用指南
│   ├── python_environment.md        # Python 环境管理
│   └── api_handler.md               # API Handler 系统
│
├── pyproject.toml                   # Python 项目配置（项目根目录）
├── package.json                     # Node.js 依赖
├── pnpm-lock.yaml                   # 依赖锁定文件
├── CLAUDE.md                        # LLM 开发辅助指南
├── README.md                        # 项目主文档（中文）
└── README_EN.md                     # 项目主文档（英文）
```

### 关键目录说明

| 路径 | 说明 | 修改频率 |
|------|------|---------|
| `src/views/` | 页面级组件（路由目标） | ⭐⭐⭐ 高 |
| `src/components/` | 可复用 UI 组件 | ⭐⭐⭐ 高 |
| `src/lib/stores/` | Zustand 状态管理 | ⭐⭐⭐ 高 |
| `src/lib/services/` | API 服务层 | ⭐⭐⭐ 高 |
| `backend/handlers/` | Python API 处理器 | ⭐⭐⭐ 高 |
| `backend/models/` | Python 数据模型 | ⭐⭐ 中 |
| `src/lib/client/` | 自动生成，勿修改 | ❌ 不修改 |
| `.venv/` | Python 虚拟环境，勿提交 | ❌ 不修改 |

## 调试技巧

### 调试前端

#### 使用浏览器开发者工具

```bash
# 1. 启动 Tauri 应用
pnpm tauri dev

# 2. 右键菜单 → 检查（在应用中查看源代码和调试）
```

#### 查看 TypeScript 类型错误

```bash
# 检查 TypeScript 编译错误
pnpm tsc

# 观察模式（修改时自动检查）
pnpm tsc --watch
```

#### 调试 Zustand 状态

```typescript
// 在组件中添加调试日志
const store = useActivityStore((state) => {
  console.log('Timeline data:', state.timelineData)
  return state
})
```

### 调试后端

#### 查看 FastAPI 文档

```bash
# 启动 FastAPI 服务器
uvicorn app:app --reload

# 访问 Swagger UI
# http://localhost:8000/docs
```

#### 添加调试日志

```python
# backend/handlers/my_handler.py
import logging

logger = logging.getLogger(__name__)

@api_handler(body=MyRequest)
async def my_handler(body: MyRequest):
    logger.debug(f"Received request: {body}")
    # ... 处理逻辑
    logger.debug(f"Response: {result}")
    return result
```

#### 查看 Python 堆栈跟踪

```bash
# 如果 Tauri 应用崩溃，查看 Rust 控制台输出
# 其中会显示 Python 异常堆栈跟踪
```

## 故障排除

### 问题 1：`pnpm setup` 失败

**症状：** 初始化时 Python 环境配置失败

**解决方案：**

```bash
# 1. 检查 uv 是否安装
uv --version

# 2. 手动初始化 Python 环境
uv sync

# 3. 检查 Python 版本
python --version  # 需要 3.14+

# 4. 清理并重新初始化
rm -rf .venv
uv sync
```

### 问题 2：PyTauri 客户端未更新

**症状：** 修改了 Python 处理器，但 TypeScript 客户端未更新

**解决方案：**

```bash
# 1. 确保已更新 Python 代码
# 2. 重新同步后端
pnpm setup-backend

# 3. 重启 Tauri 开发模式
pnpm tauri dev

# 4. 或手动重新生成
pnpm tauri build --ci
```

### 问题 3：前端无法连接后端

**症状：** API 调用超时或连接失败

**解决方案：**

```bash
# 1. 确保 Tauri 应用正在运行
# 2. 检查 Rust 编译是否成功（查看终端输出）
# 3. 查看浏览器开发者工具 → Network 标签
# 4. 如果使用 FastAPI，确保端口正确

# 测试 FastAPI 连接
curl http://localhost:8000/docs
```

### 问题 4：国际化翻译不一致

**症状：** `pnpm check-i18n` 报告错误

**解决方案：**

```bash
# 1. 检查错误信息
pnpm check-i18n

# 2. 手动添加缺失的翻译
# 在 src/locales/zh-CN.ts 中添加对应的键值对

# 3. 重新验证
pnpm check-i18n
```

### 问题 5：Rust 编译错误

**症状：** `pnpm tauri dev` 显示 Rust 编译错误

**解决方案：**

```bash
# 1. 更新 Rust 工具链
rustup update

# 2. 清理构建产物
pnpm clean

# 3. 重新启动
pnpm tauri dev
```

### 问题 6：模块导入错误

**症状：** `ModuleNotFoundError: No module named 'xxx'`

**解决方案：**

```bash
# 1. 确保新模块在 src-tauri/python/ido_app/__init__.py 中导入
# 2. 重新同步 Python 环境
pnpm setup-backend

# 3. 重启应用
pnpm tauri dev
```

### 问题 7：Windows 换行符问题

**症状：** Git 显示大量文件被修改，但实际内容没有变化，或在协作时出现换行符冲突

**原因：** Windows 和 macOS/Linux 系统使用不同的换行符（Windows 使用 CRLF，而 Unix 系统使用 LF）

**解决方案：**

```bash
# 1. 配置 git 不自动转换换行符
git config core.autocrlf false

# 2. 如果已经存在问题，可以规范化当前仓库的换行符
git add .
git commit -m "Normalize line endings"

# 3. 全局配置（推荐所有项目都使用）
git config --global core.autocrlf false
```

**预防措施：**
- 在克隆任何新项目后立即运行 `git config core.autocrlf false`
- 使用支持统一换行符显示的编辑器（如 VS Code）
- 在 `.gitattributes` 文件中统一配置换行符规则（如果项目需要）

## 最佳实践

### ✅ 开发工作流

1. **做改变前**
   - 确保在正确的分支上
   - 拉取最新的 `main` 分支代码
   - 运行 `pnpm setup` 确保依赖最新

2. **开发过程中**
   - 使用 `pnpm format` 保持代码格式一致
   - 经常测试改变（`pnpm dev` 或 `pnpm tauri dev`）
   - 添加必要的类型定义（利用 TypeScript 严格模式）

3. **提交前**
   - 运行 `pnpm lint` 检查代码质量
   - 运行 `pnpm check-i18n` 验证翻译
   - 编写清晰的 Git 提交信息

### ✅ 调试最佳实践

- 使用浏览器开发者工具调试前端
- 使用 FastAPI 文档调试后端 API
- 添加 TypeScript 类型，让编译器帮助找错误
- 使用 Zustand DevTools 调试状态变化

### ✅ 性能优化

- 使用 `React.memo` 避免不必要的重新渲染
- 使用 Zustand 选择器精确订阅状态
- 对长列表使用虚拟滚动
- 检查浏览器性能 Tab 识别瓶颈

## 获取帮助

- 📖 查看项目的其他文档：[docs/](../)
- 🐛 报告 Bug：[GitHub Issues](https://github.com/TexasOct/iDO/issues)
- 💡 讨论问题：[GitHub Discussions](https://github.com/TexasOct/iDO/discussions)
- 📞 查看 [CLAUDE.md](../CLAUDE.md) 了解详细的架构和设计模式
